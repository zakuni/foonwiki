extends layout

block content
  .row
    .small-12.column
      h1 All Pages
      ul
        each page in pages
          li
            if page.get("name")
              a(href=`/?id=${page.id}`) #{page.get("name")}
            else
              a(href=`/?id=${page.id}`) (no title)
